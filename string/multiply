//思路：
// 1. 下翻转数据
// 2. 按位相乘
// 3. 将乘得的结果进行错位相加，模拟乘法的笔算过程
//https://leetcode.cn/problems/multiply-strings/
class Solution 
{
public:
  void MulItem(string &tmp, string &num1, char a)
  {
    int i = 0, sign=0;
    int mul = 0;
    while(i < num1.size())
    {
      mul = (num1[i]-'0') * (a-'0') + sign;
      if(mul >= 10)
      {
        sign = mul / 10;
        mul %= 10;
      }
      else
        sign = 0;
      tmp.push_back(mul+'0');
      i++;
    }    

    if(sign > 0)
      tmp.push_back(sign+'0'); 
  }   

  //对应为相加，sign进位采用引用传递
  int AddItem(int a, int b, int &sign)
  {
    int add = a+b+sign;
    if(add >= 10)
    {
      sign = 1;
      add -= 10;
    }
    else
      sign = 0;
    return add;
  }
  //错位相加
  void MoveAdd(string &result, string &tmp, int k)
  {
    int i, j;
    i = k;
    j = 0;
    int sign = 0;
    while(i<result.size() &&j<tmp.size())
    {
      result[i] = AddItem(result[i]-'0', tmp[j]-'0', sign) + '0';
      i++;
      j++;
    }
    while(i<result.size() && sign)
    {
      result[i] = AddItem(result[i]-'0', 0, sign)+'0';
      i++;
    }
    while(j < tmp.size())
    {
      int v = AddItem(0, tmp[j]-'0', sign);
      result.push_back(v+'0');
      j++;
    }
    if(sign)
      result.push_back(sign+'0');
  }

  string multiply(string num1, string num2) 
  {
  	 //先翻转数据，方便进位处理
     reverse(num1.begin(), num1.end());
     reverse(num2.begin(), num2.end());
     string tmp, result;
     for(int i=0; i<num2.size(); ++i)
     {
     	 //使用num2的每一个数据乘以num1
       MulItem(tmp, num1, num2[i]);
       //将乘得的结果进行错位相加
       MoveAdd(result, tmp, i);
       tmp.clear();
     }    
     while(result.size()!=1 && result.back()=='0')
      result.pop_back();
		 //翻转数据，恢复数据
     reverse(result.begin(), result.end());
     return result;
  }

};
//这个是我自己一开始写的，复用用的相加
/*class Solution {
    string addStrings(string num1, string num2) {
        string str;
        int pos = 0,next = 0;
        int n1 = num1.size() - 1, n2 = num2.size() - 1;
        while(n1 >= 0 || n2 >= 0){
            int nu1 = n1>=0 ? num1[n1] - '0' : 0;
            int nu2 = n2>=0 ? num2[n2] - '0' : 0;
            pos = nu1 + nu2 + next;
            next = pos/10;
            pos %= 10;
            str.insert(str.begin(), '0' + pos);
            --n1,--n2;
        }
        if(next)
            str.insert(str.begin(), '0' + next);
        return str;
    }
public:
    string multiply(string num1, string num2) {
        if(num1 == "0" || num2 == "0") return "0";
        string str;
        int n1 = num1.size()-1;
        while(n1 >= 0 ){
            string tmp;
            int count = num1.size() - 1 -n1;
            //确定位数，例如10*，100*
            while(count-- > 0){
                tmp.insert(tmp.begin(),'0');
            }
            int nu1 = n1>=0 ? num1[n1] - '0' : 0;
            int pos = 0, next = 0;
            for(int n2 = num2.size() - 1; n2 >= 0; n2--){//模拟一个位*另一个串
                int nu2 = n2>=0 ? num2[n2] - '0' : 0;
                //cout<<nu1<<' '<< nu2<<' '<<next<<endl;
                pos = nu1 * nu2 + next;
                //cout<<pos<<endl;
                next = pos / 10;
                pos%=10;
                tmp.insert(tmp.begin(),'0' + pos);
                //cout<<tmp<<endl;
            }
            if(next)
                tmp.insert(tmp.begin(), '0' + next);
                //cout<<tmp<<endl;
                //cout<<str<<endl;
            str = addStrings(str,tmp);//两个相加,
            n1--;
        }
        //str.erase(str.end()-1);
        return str;
    }
};*/
